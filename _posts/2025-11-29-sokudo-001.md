---
tags:
  - bac
title: Bugforge - Sokudo 001
categories:
  - writeups
  - bugforge
date: 2025-11-29T12:00:00
author: tom
description: Finding and abusing a broken access control vulnerability to make unauthorized changes on the server
image: /assets/img/sokudo-thumbnail.png
---
## Overview

This version of the Sokudo lab is an easy rated lab on [Bugforge.io](https://Bugforge.io). It involves abusing a broken access control vulnerability to modify typing statistics on a speed-typing application.

## Attack Path

After poking around the app a little bit, we see there is a stats page that shows everyone’s fastest typing scores.

![](assets/img/2025-11-29-sokudo-001/file-20251129103746425%201.png){: .shadow .rounded-corners }
_Viewing the Sokudo statistics page_

Looking at this request in our proxy, we see that the stats for our user are returned.

![](assets/img/2025-11-29-sokudo-001/file-20251129103746425.png){: .shadow .rounded-corners }
_Looking at the data returned from the call to `/api/stats`_

If access control is not correctly enforced, it may be possible to tamper with our stats. Often, testing different HTTP verbs may reveal access control misconfigurations. In this case, we can try a `PUT` request.

![](assets/img/2025-11-29-sokudo-001/file-20251129103746423.png){: .shadow .rounded-corners }
_Changing the request from `GET` to `PUT` to update our stats_

As you can see, sending a `PUT` request with the JSON content returned from the previous `GET` request successfully updates the stats, and we get the flag.

There’s one big thing to keep in mind with this lab — when sending the `PUT` request, make sure to change the `Content-Type` header to `application/json`. Otherwise you’ll get some weird behavior.

That’s it for this lab! See ya next time!